/* =========================================================
   CONFIGURATION SUPABASE — RDC Jeunes Opportunités
   =========================================================
   Remplace les deux valeurs ci-dessous par celles de ton
   projet, disponibles dans Supabase :
   Project Settings (icône engrenage) > API
   - "Project URL"      -> SUPABASE_URL
   - "anon public" key   -> SUPABASE_ANON_KEY
   Ces deux valeurs sont conçues pour être publiques
   (visibles dans le code du site) — ce n'est pas la clé secrète.
   ========================================================= */

const SUPABASE_URL = "https://rdc-jeunes-opportunites.supabase.co";
const SUPABASE_ANON_KEY = "tvikvysdfbrcwmnnlgwp"
